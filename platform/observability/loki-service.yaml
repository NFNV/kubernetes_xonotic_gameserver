apiVersion: v1
kind: Service
metadata:
  name: xonotic-loki
  namespace: xonotic-observability
spec:
  type: ClusterIP
  selector:
    app: xonotic-loki
  ports:
    - name: http
      port: 3100
      targetPort: http
      protocol: TCP
