apiVersion: v1
kind: Service
metadata:
  name: xonotic-node-exporter
  namespace: xonotic-observability
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: xonotic-node-exporter
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
      protocol: TCP
