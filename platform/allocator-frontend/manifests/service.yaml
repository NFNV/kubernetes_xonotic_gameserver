apiVersion: v1
kind: Service
metadata:
  name: xonotic-allocator-frontend
  namespace: xonotic-allocator-backend
spec:
  type: ClusterIP
  selector:
    app: xonotic-allocator-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
